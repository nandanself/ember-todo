<h2>TODO APP</h2>

<div class="todo-container">
    {{todo-item}}
</div>

<button class="fixed-button" {{action "openAddModal"}}>+</button>

{{#if isAddModalOpen}}
    {{add-modal closingProperty=isAddModalOpen}}
{{/if}}

{{outlet}}
